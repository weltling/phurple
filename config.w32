// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("purple", "for purple support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("purple", "enable purple support", "no");

if (PHP_PURPLE != "no") {
	EXTENSION("purple", "purple.c");
}

